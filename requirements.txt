# Core
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
Pillow>=10.0.0

# OCR — pinned for CPU compatibility
paddlepaddle==2.5.2
paddleocr==2.7.3

# Document AI
transformers>=4.40.0
torch>=2.0.0
torchvision>=0.15.0

# LLM (loaded lazily — may be slow on CPU)
accelerate>=0.27.0

# Embeddings & categorisation
sentence-transformers>=2.7.0
scikit-learn>=1.3.0

# Database
supabase>=2.4.0

# HuggingFace Hub (for private model loading)
huggingface-hub>=0.22.0

# Utilities
python-dateutil>=2.8.0
